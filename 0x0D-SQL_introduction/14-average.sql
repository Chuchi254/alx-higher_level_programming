-- Computes the score average of all records in the table second_table
-- This script calculates the average score of all records in the table second_table and names the result column as average

SELECT AVG(score) AS average
FROM second_table;
