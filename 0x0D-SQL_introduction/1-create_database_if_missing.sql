-- Create the database hbtn_0c_0 if it does not already exist
-- Creates the database hbtn_0c_0 in your MYSQL server, ensuring it doesn'tfail if the database already exists

CREATE DATABASE IF NOT EXISTS hbtn_0c_0;
